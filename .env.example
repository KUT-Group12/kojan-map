# Environment variables template
# Copy this file to .env and fill in your actual values

# Google OAuth Configuration
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# JWT Secret (REQUIRED - use a strong random string)
# Generate with: openssl rand -base64 32
JWT_SECRET_KEY=your-random-secret-key-at-least-32-characters-long

# Database configuration is managed in compose.yaml
# These values are already set in the Docker Compose file:
# DB_HOST=db
# DB_PORT=3306
# DB_USER=root
# DB_PASSWORD=root
# DB_NAME=kojanmap
